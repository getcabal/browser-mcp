{
  "name": "local-browser-mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only MCP bridge for the Local Browser MCP extension",
  "bin": { "local-browser-mcp": "./dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "npm run build && node test/e2e.mjs && node test/local-only-audit.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.0.0"
  },
  "engines": { "node": ">=18" }
}
