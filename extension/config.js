/**
 * Per-profile deployment configuration. The packager stamps a copy of this
 * file for each fleet profile; chrome.storage.local overrides these defaults
 * at runtime (set via the options page).
 */
export default { port: 19889, profile: null };
